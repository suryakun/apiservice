'use strict';

angular.module('roomApp')
    .controller('HeaderCtrl', function ($scope, $location, Auth, $http) {
    // $http.get('/api/users/get-my-class').success(function (data) {
    //     console.log(data);
        // $scope.school = data;
    // });
});
